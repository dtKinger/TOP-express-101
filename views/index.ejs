<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css'>
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% if (locals.links){ %>
    <ul>
      <% links.forEach((link) => { %>
        <li><a href="<%= link.url%>"><%= link.page %></a></li>
      <% }) %>
    </ul>
    <% } else { %>

    <% } %>
  </body>
</html>